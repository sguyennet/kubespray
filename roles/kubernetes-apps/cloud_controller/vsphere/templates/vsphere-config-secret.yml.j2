apiVersion: v1
kind: Secret
metadata:
  name: cpi-global-secret
  namespace: kube-system
stringData:
  {{ vsphere_vcenter_ip }}.username: "{{ vsphere_user }}"
  {{ vsphere_vcenter_ip }}.password: "{{ vsphere_password }}"